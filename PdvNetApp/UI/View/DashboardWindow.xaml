<Window x:Class="PdvNetApp.UI.View.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Relatórios e Dashboard"
        Height="400" Width="600"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Relatórios de Estoque" FontSize="20" FontWeight="Bold" Margin="0 0 0 20"/>

        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0 20 0 10">
            <TextBlock Text="{Binding TotalProdutos, StringFormat='Total de produtos: {0}'}" FontSize="16" Margin="0 5"/>
            <TextBlock Text="{Binding ValorTotalEstoque, StringFormat='Valor total do estoque: R$ {0:N2}'}" FontSize="16" Margin="0 5"/>
        </StackPanel>

        <GroupBox Grid.Row="3" Header="⚠️ Produtos com baixo estoque (menos de 5 unidades)">
            <DataGrid ItemsSource="{Binding ProdutosBaixoEstoque}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                    <DataGridTextColumn Header="Quantidade" Binding="{Binding Quantidade}" Width="100"/>
                    <DataGridTextColumn Header="Preço" Binding="{Binding Preco, StringFormat=R$ {0:N2}}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</Window>
